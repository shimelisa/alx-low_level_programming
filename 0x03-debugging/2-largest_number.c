#include "main.h"

/**
* largest_numbr - returns the largest of 3 numbers
* @a: firs integer
* @b: second integer
* @c: third integer
* Return: largest number
*/

int largest_number(int a, int b, int c)
{
	int largest;

	if (a >= b && a >= C)
		{
			largest = a;
		}
	else if (b >= a && b >= C)
	{
		largest = b;
	}
	else
	{
		largest = c;
	}

	return (large);
}
