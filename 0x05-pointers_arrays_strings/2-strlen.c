#include <stdio.h>
#include "main.h"
/**
* _strlen - function that returns lengh of sting.
* @s: sting to be checked for length
* Return: integer
*/
int _strlen(char *s)
{
	int leng = 0;

	while (*s != '\0')
	{
		s++;
		len++;
	}

	return (len);
}
